[package]
name = "rust"
version = "0.1.0"
authors = ["karashiiro <49822414+karashiiro@users.noreply.github.com>"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
